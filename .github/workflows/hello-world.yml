name: "Greet User"

on: push

jobs:
    greet:
        runs-on: ubuntu-latest
        steps:
            - name: greet User
              run: echo "Hello world"
            - name: bye User
              run: echo "Bye world"